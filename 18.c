#include <stdio.h>
float main ()
{
    float a,b ;
    a=10.8;
    b=3.3;
    if (a<b)
    {
        printf("a is smaller then b");
    }
    else
    {
        printf("b is smaller then a");
    }
    return 0;
}
